- VentraPath frontend-derived architecture is now locked on disk from actual current frontend code: current implemented scope ends at Blueprint + Phase 1 Brand + Phase 2 Legal + Phase 3 Finance; later phases are not yet built and must remain provisional.
- Product rule saved repeatedly in work: the unique twist is essential and must appear in the first section describing the company in Blueprint/Phase 0 because it is the emotional ignition point driving excitement and conversion into the paywall.
- Added/updated docs during this session: `projects/ventrapath/specs/data-models/blueprint-business-page.md`, `phase1-brand-page.md`, `phase2-legal-page.md`, `phase3-finance-page.md`, and `projects/ventrapath/docs/frontend-derived-architecture.md`; `projects/ventrapath/specs/backend-spec.md` was updated to reflect frontend-derived architecture.
- Git checkpoint was made for the architecture/spec/docs work: commit `9358754` with message `VentraPath: derive architecture from implemented frontend`.
- Backend direction changed from generated-content-only toward phase records with three layers: generated/reference content, userState, and progress/completion state.
- Brand and Legal phase builders were updated to return `content`, `generated`, and `userState`; Finance backend generation was added so Phase 3 is no longer just a placeholder in design terms.
- Phase ladder/backend honesty improved so Finance is treated as part of current implemented frontend scope rather than pretending it is locked because it was unimplemented on the backend.
- Real blocker discovered during smoke testing: persistence layer was stripping workflow state. JSON/Postgres phase instance persistence initially stored only `generatedContent`/`tasks`, so route-level support outpaced persistence.
- Follow-up fix work started for persistence: added migration `projects/ventrapath/backend/migrations/0008_add_phase_instance_workflow_state.sql` to add `user_state` and `progress` columns to `phase_instances`; JSON and Postgres stores plus phase route write path were being updated to persist workflow state, but live verification was still pending because the backend needed another restart and rerun of the smoke test.
- Important next step for future self: restart backend, apply migration if needed, and rerun end-to-end smoke test through Phase 3 Finance to verify stored `userState`/`progress` actually survive persistence.
