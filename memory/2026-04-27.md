# 2026-04-27

- VentraPath backend now runs successfully against a local embedded Postgres instance on `127.0.0.1:5432` with database `ventrapath`; backend `.env` uses `PERSISTENCE_DRIVER=postgres` and local `DATABASE_URL`.
- Added repeatable local Postgres support in `projects/VentraPath/backend/`: env file loading, `scripts/local-postgres.js`, npm scripts for `postgres:start`, `postgres:check`, `postgres:stop`, and migration apply support.
- Applied backend migrations `0001` through `0005` successfully against Postgres and verified real DB tables including `users`, `projects`, `agent_runs`, `blueprint_versions`, and `schema_migrations`.
- Fixed Postgres-backed API loop issue caused by non-UUID dev user IDs by switching the dev default to a valid UUID shape, validating UUIDs in Postgres mode, and ensuring the user row exists before project/blueprint operations.
- Verified the real Postgres-backed backend loop end-to-end: create project, generate blueprint, fetch blueprint versions, and fetch project with `blueprint_ready` state.
- Checkpointed the local Postgres setup in git with commit `0650653`.
- Gaz started providing screenshots for guided phases beyond the blueprint so backend contracts can match the actual UI instead of generic assumptions.
- Phase 1 Brand UI is now confirmed as a 5-step accordion workflow with helper/example panels, interactive inputs, provider/action cards, and completion tracking; docs updated and checkpointed in commit `4482ac5`.
- Phase 1 Brand observed steps: Business Name, Brand Positioning, Logo & Visual Identity, Domain & Email Setup, and Social Handles.
- Phase 2 Legal UI is now confirmed as a 6-step accordion workflow with country-specific context, progress tracking, official/external links, checklists, template actions, and helper/example panels.
- Gaz explicitly stressed that Legal must be country specific, must connect users to the correct websites, and must include disclaimers; this is now locked into VentraPath docs.
- Updated VentraPath docs so Legal must use the correct jurisdiction, official authority links where possible, visible disclaimer copy, and no bluffing when uncertain; checkpointed in commit `272a819`.
- Phase 2 Legal observed steps: Choose Business Structure, Register Your Business, Get Your ABN / local identifier, Set Up Taxes, Business Bank Account, and Basic Legal Protection.
- Current product-direction insight: guided phases should be modeled as `phase shell + ordered step modules` rather than a generic blob of content plus tasks.
