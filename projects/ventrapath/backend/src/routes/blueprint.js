import { notImplemented } from '../lib/http.js';

export function handleGenerateBlueprint(req, res, projectId) {
  return notImplemented(res, `VP-BE-007 pending: blueprint generation for ${projectId} is scaffolded but not connected yet.`);
}

export function handleGetBlueprint(req, res, projectId) {
  return notImplemented(res, `VP-BE-009 pending: blueprint retrieval for ${projectId} is scaffolded but not connected yet.`);
}
