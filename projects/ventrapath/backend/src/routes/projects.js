import { notImplemented } from '../lib/http.js';

export function handleListProjects(req, res) {
  return notImplemented(res, 'VP-BE-005 pending: list projects endpoint is scaffolded but not connected yet.');
}

export function handleCreateProject(req, res) {
  return notImplemented(res, 'VP-BE-005 pending: create project endpoint is scaffolded but not connected yet.');
}

export function handleGetProject(req, res, projectId) {
  return notImplemented(res, `VP-BE-005 pending: project detail for ${projectId} is scaffolded but not connected yet.`);
}
