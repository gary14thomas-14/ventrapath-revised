# VentraPath Scripts

This folder is for repeatable helper scripts.

Suggested script groups:
- `export-ingest/` — frontend export ingestion/parsing helpers
- `local-test/` — local sanity checks and smoke-test helpers
- `repo-sync/` — repo preparation or sync helpers

Rule: if a local workflow gets repeated, it should probably become a script.
