# Server-side proxy target used by Next rewrites.
# Local default in next.config.mjs is http://127.0.0.1:4000 when unset.
BACKEND_URL=http://127.0.0.1:4000

# Optional client override. Leave unset when using the proxy.
# NEXT_PUBLIC_API_BASE_URL=/api
